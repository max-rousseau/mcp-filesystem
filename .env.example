# Docker Compose Parameters
# Copy to .env and adjust as needed

# Host interface to bind (127.0.0.1 = localhost only, 0.0.0.0 = all interfaces)
MCP_HOST=127.0.0.1

# Host port to expose the MCP service
MCP_PORT=8000
